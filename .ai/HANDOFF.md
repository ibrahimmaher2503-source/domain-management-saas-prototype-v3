# Handoff

The root is the production Laravel application with migrated mock UI pages for Overview, Domains, Domain Detail, Transfers, SSL, Billing, and Settings. The original UI is under `prototype/`. Product models, registrar integration, checkout, payments, domain management, and admin remain intentionally unimplemented.
