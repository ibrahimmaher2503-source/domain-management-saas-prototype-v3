# Handoff

The root is the production Laravel application. The original UI is under `prototype/`. Product models, registrar integration, checkout, payments, domain management, and admin remain intentionally unimplemented.
