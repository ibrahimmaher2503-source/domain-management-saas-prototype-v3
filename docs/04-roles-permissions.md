# Roles and Permissions

- Customer: may access only resources whose `user_id` matches the authenticated user.
- Internal admin: future separate application surface; not implemented here.

Server-side authorization is mandatory; hidden UI is not authorization.
