# Database Schema Direction

Laravel authentication tables exist. Future resources use direct ownership such as `domains.user_id`, `orders.user_id`, `payments.user_id`, `transfers.user_id`, and `certificate_orders.user_id`. No product-domain migrations are part of this milestone.
