# Definition of Done

Requested scope is implemented, secrets are absent from version control, relevant tests and build pass, documentation matches the code, and unverified external-runtime checks are reported honestly.
