# React prototype

Reference React + Vite implementation of the same product UI as the zero-install prototype in the parent directory.

```bash
npm install
npm run dev
```

No backend is connected. Mutations are local UI-state demonstrations intended for design/implementation handoff.
