import type { DomainDetail } from '@/types/domain';
export default function DomainSummary({ domain }: { domain: DomainDetail }) { const items = [['Expiration', domain.expiresAt], ['Auto renew', domain.autoRenew ? 'On' : 'Off'], ['Transfer lock', domain.transferLocked ? 'Locked' : 'Unlocked'], ['Privacy', domain.privacyStatus], ['Transfer', domain.transferStatus], ['SSL', domain.sslStatus]]; return <div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-3">{items.map(([label, value]) => <div className="rounded-lg border border-[#e5e5e5] bg-white p-4" key={label}><p className="text-xs text-[#737373]">{label}</p><p className="mt-1 font-medium">{value}</p></div>)}</div>; }
