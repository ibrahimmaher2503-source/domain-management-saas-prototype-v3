export const domainTabs = ['Overview', 'Nameservers', 'DNS', 'Security', 'Renewal', 'Transfers', 'SSL', 'Billing', 'Activity'] as const;
export type DomainTab = typeof domainTabs[number];
export default function DomainTabs({ active, onChange }: { active: DomainTab; onChange: (tab: DomainTab) => void }) { return <div className="mb-5 overflow-x-auto border-b" role="tablist" aria-label="Domain management sections"><div className="flex min-w-max gap-1">{domainTabs.map((tab) => <button type="button" role="tab" aria-selected={active === tab} key={tab} onClick={() => onChange(tab)} className={`min-h-11 border-b-2 px-3 py-3 text-sm font-medium focus:outline-none focus:ring-2 focus:ring-inset focus:ring-black ${active === tab ? 'border-neutral-900 text-neutral-900' : 'border-transparent text-neutral-600 hover:text-neutral-900'}`}>{tab}</button>)}</div></div>; }
