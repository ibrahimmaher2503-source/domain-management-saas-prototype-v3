import { Link } from '@inertiajs/react';
type Transfer={id:number;status:string;requested_at:string|null};
export default function TransferPanel({domainId,transferLocked,transfer}:{domainId:number;transferLocked:boolean|null;transfer?:Transfer|null}) { return <section className="rounded-xl border border-[#e5e5e5] bg-white p-5"><h2 className="font-semibold">Transfer this domain away</h2><ol className="mt-4 list-decimal space-y-2 pl-5 text-sm text-[#525252]"><li>Unlock the domain{transferLocked===false?' — currently unlocked.':'.'}</li><li>Retrieve the Auth/EPP code securely.</li><li>Start the transfer at your new registrar.</li></ol><Link href={route('domains.show',{domain:domainId,tab:'security'})} className="mt-5 inline-block rounded-md bg-[#171717] px-4 py-2 text-sm text-white">Open Security</Link>{transfer&&<div className="mt-6 border-t pt-5"><h3 className="font-medium">Incoming transfer</h3><p className="mt-2 text-sm capitalize">Status: {transfer.status.replaceAll('_',' ')}</p><Link href={route('transfers.show',transfer.id)} className="mt-3 inline-block text-sm underline">View transfer</Link></div>}</section>; }
