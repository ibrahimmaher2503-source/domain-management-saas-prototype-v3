import type { ReactNode } from 'react';
export default function PageHeader({ eyebrow, title, description, actions }: { eyebrow?: string; title: string; description?: string; actions?: ReactNode }) { return <div className="mb-6 flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div>{eyebrow && <span className="text-xs font-semibold uppercase tracking-[.14em] text-[#a3a3a3]">{eyebrow}</span>}<h1 className="mt-1 text-2xl font-semibold tracking-tight text-[#171717]">{title}</h1>{description && <p className="mt-1 max-w-2xl text-sm text-[#737373]">{description}</p>}</div>{actions && <div className="flex gap-2">{actions}</div>}</div>; }
