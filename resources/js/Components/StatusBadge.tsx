const tones: Record<string, string> = { active: 'bg-green-50 text-green-700', completed: 'bg-green-50 text-green-700', paid: 'bg-green-50 text-green-700', connected: 'bg-green-50 text-green-700', issued: 'bg-green-50 text-green-700', expiring: 'bg-amber-50 text-amber-700', action_required: 'bg-amber-50 text-amber-700', processing: 'bg-blue-50 text-blue-700', pending: 'bg-blue-50 text-blue-700', transfer_pending: 'bg-blue-50 text-blue-700', failed: 'bg-red-50 text-red-700', expired: 'bg-red-50 text-red-700', cancelled: 'bg-neutral-100 text-neutral-600', off: 'bg-neutral-100 text-neutral-600', unsupported: 'bg-neutral-100 text-neutral-500' };
export default function StatusBadge({ status }: { status: string }) { const key = status.trim().toLowerCase().replaceAll(' ', '_'); const label = key.split('_').map(word => word[0]?.toUpperCase() + word.slice(1)).join(' '); return <span className={`inline-flex whitespace-nowrap rounded-full px-2.5 py-1 text-xs font-medium ${tones[key] ?? 'bg-neutral-100 text-neutral-600'}`}>{label}</span>; }
