import AdminLayout from '@/Layouts/AdminLayout';
import { Link } from '@inertiajs/react';

export default function Pricing({ pricing, provider_price }: any) {
    return <AdminLayout title="Pricing"><div className="overflow-hidden rounded-xl border bg-white"><table className="w-full text-left text-sm"><thead className="border-b bg-neutral-50 text-xs uppercase text-neutral-500"><tr><th className="px-5 py-3">Input</th><th className="px-5 py-3">Configuration source</th><th className="px-5 py-3">Effective value</th><th className="px-5 py-3">State</th></tr></thead><tbody className="divide-y">{pricing.map((item: any) => <tr key={item.name}><td className="px-5 py-4 font-medium">{item.name}</td><td className="px-5 py-4 text-neutral-500">{item.source}</td><td className="px-5 py-4">{item.value}</td><td className="px-5 py-4">{item.enabled ? 'Enabled' : 'Disabled'}</td></tr>)}</tbody></table></div><section className="mt-5 rounded-xl border bg-white p-5"><div className="flex items-center justify-between"><div><h2 className="font-semibold">Current .com provider registration price</h2><p className="text-xs text-neutral-500">Fetched read-only from OnlineNIC on request.</p></div><Link href={route('admin.pricing', { refresh_price: 1 })} className="rounded-lg border px-3 py-2 text-sm">Fetch price</Link></div>{provider_price && <p className="mt-4 text-xl font-semibold">{provider_price.unavailable ? 'Unavailable' : `${provider_price.amount} ${provider_price.currency}`}</p>}</section><p className="mt-4 text-xs text-neutral-500">Read-only configuration view. This page never edits environment settings.</p></AdminLayout>;
}
