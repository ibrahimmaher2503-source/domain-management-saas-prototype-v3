import AppLayout from '@/Layouts/AppLayout';
import PageHeader from '@/Components/PageHeader';
import StatusBadge from '@/Components/StatusBadge';
import { domains } from '@/mocks/domains';
import { Link } from '@inertiajs/react';

const metrics = [['Total domains', '4', 'Across your account'], ['Active', '3', 'No action required'], ['Expiring soon', '1', 'Within 30 days'], ['Transfers in progress', '1', 'Registrar processing']];
export default function Index() { return <AppLayout title="Overview"><PageHeader eyebrow="Portfolio" title="Overview" description="A calm view of your domains, renewals, transfers, and services." actions={<Link href={route('domains')} className="rounded-md bg-[#171717] px-4 py-2 text-sm font-medium text-white">View domains</Link>} /><div className="mb-6 grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{metrics.map(([label, value, note]) => <section className="rounded-xl border border-[#e5e5e5] bg-white p-4" key={label}><p className="text-xs font-medium text-[#737373]">{label}</p><p className="mt-2 text-2xl font-semibold">{value}</p><p className="mt-1 text-xs text-[#a3a3a3]">{note}</p></section>)}</div><div className="grid gap-6 xl:grid-cols-[1.6fr_1fr]"><section className="overflow-hidden rounded-xl border border-[#e5e5e5] bg-white"><div className="flex items-center justify-between border-b border-[#e5e5e5] px-5 py-4"><h2 className="font-semibold">Recent domains</h2><Link href={route('domains')} className="text-sm text-[#737373]">View all →</Link></div><div className="divide-y divide-[#e5e5e5]">{domains.map((domain) => <Link href={route('domains.show', domain.id)} className="flex items-center justify-between gap-4 px-5 py-4 hover:bg-[#fafafa]" key={domain.id}><div><p className="font-medium">{domain.name}</p><p className="mt-1 text-xs text-[#a3a3a3]">Expires {domain.expiresAt}</p></div><StatusBadge status={domain.status} /></Link>)}</div></section><section className="rounded-xl border border-[#e5e5e5] bg-white p-5"><p className="text-xs font-semibold uppercase tracking-[.14em] text-[#a3a3a3]">Needs attention</p><h2 className="mt-2 text-lg font-semibold">One domain is expiring soon</h2><p className="mt-2 text-sm text-[#737373]">Review northstar.io before its renewal date.</p><Link href={route('domains.show', 2)} className="mt-5 inline-flex rounded-md border border-[#d4d4d4] px-3 py-2 text-sm font-medium">Review domain</Link></section></div></AppLayout>; }
