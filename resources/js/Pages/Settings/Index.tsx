import AppLayout from '@/Layouts/AppLayout';
import PageHeader from '@/Components/PageHeader';
import { Link } from '@inertiajs/react';

export default function Index() { return <AppLayout title="Settings"><PageHeader eyebrow="Customer account" title="Settings" description="Manage your profile, password, and account security." /><div className="grid gap-4 md:grid-cols-3">{[['Profile', 'Update your name and email address.'], ['Password', 'Choose a strong account password.'], ['Security', 'Review account protection settings.']].map(([title, copy], index) => <section className="rounded-xl border border-[#e5e5e5] bg-white p-5" key={title}><h2 className="font-semibold">{title}</h2><p className="mt-2 min-h-10 text-sm text-[#737373]">{copy}</p>{index < 2 ? <Link href={route('profile.edit')} className="mt-5 inline-flex rounded-md border border-[#d4d4d4] px-3 py-2 text-sm font-medium">Manage {title.toLowerCase()}</Link> : <button data-mock-action className="mt-5 rounded-md border border-[#d4d4d4] px-3 py-2 text-sm font-medium">Review security</button>}</section>)}</div></AppLayout>; }
