import AppLayout from '@/Layouts/AppLayout';
import PageHeader from '@/Components/PageHeader';
import StatusBadge from '@/Components/StatusBadge';
import { Link } from '@inertiajs/react';

type Transfer = { id: number; domain: string; direction: string; status: string; requested_at: string | null; updated_at: string };
export default function Index({ transfers }: { transfers: Transfer[] }) { return <AppLayout title="Transfers"><PageHeader eyebrow="Registrar operations" title="Transfers" description="Track domain transfers and any customer action required." actions={<Link href={route('transfers.create')} className="rounded-md bg-[#171717] px-4 py-2 text-sm font-medium text-white">Transfer a domain</Link>} /><div className="overflow-hidden rounded-xl border border-[#e5e5e5] bg-white"><div className="overflow-x-auto"><table className="w-full min-w-[700px] text-left text-sm"><thead className="bg-[#fafafa] text-xs uppercase text-[#737373]"><tr>{['Domain', 'Direction', 'Status', 'Requested', 'Last update', 'Action'].map((h) => <th className="px-5 py-3 font-medium" key={h}>{h}</th>)}</tr></thead><tbody className="divide-y divide-[#e5e5e5]">{transfers.map((item) => <tr key={item.id}><td className="px-5 py-4 font-medium">{item.domain}</td><td className="px-5 py-4">{item.direction === 'in' ? 'Incoming' : 'Outgoing'}</td><td className="px-5 py-4"><StatusBadge status={item.status} /></td><td className="px-5 py-4 text-[#737373]">{item.requested_at ? new Date(item.requested_at).toLocaleDateString() : 'Not submitted'}</td><td className="px-5 py-4 text-[#737373]">{new Date(item.updated_at).toLocaleString()}</td><td className="px-5 py-4"><Link href={route('transfers.show', item.id)} className="underline">View</Link></td></tr>)}</tbody></table>{!transfers.length && <p className="p-6 text-sm text-[#737373]">No transfers yet.</p>}</div></div></AppLayout>; }
